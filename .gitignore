# 参考项目，保留在本地但不入库（3.3G，含 6 个嵌套 git 仓库）
# 注意：不能叫 vendor/，否则会被 Go 当成依赖目录导致 go build 失败
_references/

# 构建产物
dist/
# 前端产物不入库，但保留占位文件让 //go:embed all:dist 在没有构建产物时也能编译
internal/web/dist/*
!internal/web/dist/.gitkeep
web/node_modules/
node_modules/

# 索引与渲染缓存（运行时生成，内容目录才是真相）
data/

# 密钥
.secrets/

# 系统文件
.DS_Store
*.log
